<div id="loginbox" class="col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
    <div class="panel panel-default">
        <div class="panel-heading">
            <div class="panel-title"><?=__('Please log in')?></div>
        </div>
        <div class="panel-body">
            <form method="post" class="form-horizontal" role="form">
                <?php
                    // CSRF
                    echo $this->formInput($this->form->get('login_csrf'));
                    // Champs de saisie
                    $username = $this->form->get('username');
                    $password = $this->form->get('password');
                    $submit = $this->form->get('submit');
                ?>
                <div style="margin-bottom: 15px;" class="input-group">
                    <span class="input-group-addon"><i class="glyphicon glyphicon-user"></i></span>
                    <?= $this->formInput($username) ?>
                </div>

                <div style="margin-bottom: 15px;" class="input-group">
                    <span class="input-group-addon"><i class="glyphicon glyphicon-lock"></i></span>
                    <?= $this->formInput($password) ?>
                </div>

                <div class="input-group">
                    <?= $this->formSubmit($submit) ?>
                    <a href="<?= $this->smartUrlFromRoute('user.forgot') ?>"><?=__('Forgot password')?></a>
                </div>
            </form>
        </div>
    </div>
</div>