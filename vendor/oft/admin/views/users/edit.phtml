<?php if ($this->create) :
    $option = array('legend' => __('Create User'));
endif; ?>
<?php if ($this->edit) :
    $option = array('legend' => __('Edit User'));
endif; ?>
<?php if ($this->view) :
    $option = array('legend' => __('View User'));
endif;

echo $this->smartForm($form, $option);

echo '<div class="form-group pull-right">';

if($this->view && $this->access['edit']) :
    echo ' <a class="btn btn-primary" role="button" '
    . 'href="'.$this->smartUrlFromRoute('users.edit', array('id' => $form->get('id_user')->getValue())).'">'.__('Modify').'</a>';
endif;
    
echo ' <a class="btn btn-default" role="button" href="'.$this->smartUrlFromRoute('users.list').'">'.__('Back').'</a>'.
    '</div>';
?>